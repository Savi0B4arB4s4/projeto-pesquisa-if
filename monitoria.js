const PromptSync = require('prompt-sync');
const soma >= 0
const quantidade = 0
var
n= inteiro
while True:
    n = int(input("Digite um número inteiro: "));
    if n == 0:
        break
    soma = soma + n
    quantidade = quantidade + 1;
console.log("Quantidade de números digitados:", quantidade);
console.log( soma);
console.log('Média: {soma/quantidade:10.2f}');